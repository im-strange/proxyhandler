from setuptools import setup, find_packages

setup(
  name = "proxyhandler",
  version = "1.0.0",
  url = "https://github.com/im-strange/proxyhandler.git",
  author = "Samuel Genoguin",
  description = "Proxy filter.",
  include_package_data = True,
  packages = find_packages(),
  package_data={}
)
